import { test, expect } from '@playwright/test';

test.describe("Lesson 7 - API Testing with Different HTTP Methods", () => {
  test("Creating new data with POST requests", async ({ page }) => {
  });

  test("Updating data with PUT requests", async ({ page }) => {
  });

  test("Partially updating data with PATCH requests", async ({ page }) => {
  });

  test("Deleting data with DELETE requests", async ({ page }) => {
  });

  test("Testing error responses and edge cases", async ({ page }) => {
  });

});
