import { test, expect } from '@playwright/test';

test.describe("Lesson 6 - Introduction to API Testing", () => {
  test("Understanding what an API is", async ({ page }) => {
  });

  test("Getting a list of data from an API", async ({ page }) => {
  });

  test("Checking response times and performance", async ({ page }) => {
  });

  test("Testing different types of data", async ({ page }) => {
  });

});
